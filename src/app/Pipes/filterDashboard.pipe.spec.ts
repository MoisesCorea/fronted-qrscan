import { FilterDashboardPipe } from './filterDashboard.pipe';

describe('FilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDashboardPipe();
    expect(pipe).toBeTruthy();
  });
});
